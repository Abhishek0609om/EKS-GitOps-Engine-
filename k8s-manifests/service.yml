apiVersion: v1
kind: Service
metadata:
  name: phoenix-app-service
spec:
  type: LoadBalancer 
  selector:
    app: phoenix-app
  ports:
  - name : http
    protocol: TCP
    port: 80
    targetPort: 5000
  - name : metrics
    protocol: TCP
    port: 8080
    targetPort: 8000
