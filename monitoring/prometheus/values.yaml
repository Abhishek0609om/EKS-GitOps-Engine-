prometheus:
  enabled: true

  prometheusSpec:

    retention: 15d

    scrapeInterval: 15s

    resources:

      requests:
        memory: 512Mi
        cpu: 250m

      limits:
        memory: 1Gi
        cpu: 500m